# U1/U2 休眠与唤醒设计方案

## 1. 目的

本文定义 U1 Display 和 U2 Control 的联动休眠、唤醒和外设电源管理方案。

目标是：

- U1 长按电源键后关闭显示，并通过串口通知 U2 进入休眠；
- U1、U2 在进入 STOP 前关闭不需要工作的外设和输出，尽可能降低功耗；
- U1 按键唤醒 U1 后，U1 通过共享 `WK_UP` 信号唤醒 U2；
- USB 插入唤醒 U2 后，U2 通过共享 `WK_UP` 信号唤醒 U1；
- 唤醒后按顺序恢复时钟、GPIO、串口、ADC、定时器和传感器；
- 唤醒不会自动启动压力泵，避免产生意外动作。

## 1.1 先看结论

本方案的核心只有两条唤醒路径：

```text
S1 按键按下  ->  U1 PA0 下降沿唤醒 U1
              ->  U1 PB5/WK_UP 输出约 5 ms 高脉冲
              ->  U2 PB7/WK_UP 上升沿唤醒 U2

USB 插入    ->  U2 PA5/USBIN 上升沿唤醒 U2
              ->  U2 PB7/WK_UP 输出约 5 ms 高脉冲
              ->  U1 PB5/WK_UP 上升沿唤醒 U1
```

`U1 PB5/WK_UP` 与 `U2 PB7/WK_UP` 是同一根硬件连线。两颗 MCU 进入 STOP 时都将本脚配置为**下拉输入 + 上升沿 EXTI**；只有已经被本地事件唤醒的一方，才可以临时把本脚改成输出并拉高，脉冲结束后立即恢复为下拉输入。

第一版只把 U1 的 S1 和 U2 的 USB 插入作为 STOP 唤醒源。加键、减键、照明键仍按原有业务使用，不直接负责从 STOP 唤醒。

实现状态：第一版代码已经加入 U1/U2 工程，包含 `CMD=0x05`、STOP 前外设停用、PA0/PA5 本地唤醒、PB5/PB7 共享唤醒和唤醒后的外设恢复。当前已完成源文件交叉编译和 Keil 工程文件检查；真实 STOP 电流、WK_UP 波形和整机按键/USB 联动仍必须在目标板上实测确认。

## 2. 第一版范围

第一版只实现两个本地唤醒源：

1. U1 电源键 S1；
2. U2 USB 插入检测 `PA5/USBIN`。

加键、减键和照明键在第一版中不作为 STOP 唤醒源。U1 被唤醒并恢复普通 GPIO 配置后，这些按键继续按原有功能工作。

这样可以先验证双 MCU 联动和功耗控制。后续如需让所有按键都能从 STOP 唤醒，需要单独检查 U1 按键矩阵与 EXTI 端口复用的限制。

## 3. 硬件连接和引脚定义

### 3.1 唤醒相关引脚

| 功能 | MCU | GPIO | 休眠状态 | 有效边沿 |
|---|---|---|---|---|
| U1 电源键 S1 | U1 | `PA0/P00` | 显示关闭后切为输入上拉 | 按下时下降沿 |
| U1 共享唤醒 | U1 | `PB5/WK_UP` | 输入下拉 | 上升沿 |
| USB 插入检测 | U2 | `PA5/USBIN` | 输入下拉 | USB 插入上升沿 |
| U2 共享唤醒 | U2 | `PB7/WK_UP` | 输入下拉 | 上升沿 |

U1 的 `PB5/WK_UP` 和 U2 的 `PB7/WK_UP` 连接在同一根 `WK_UP` 线上。休眠时两边都是下拉输入，线路默认保持低电平。

注意：U1 的 S1 并不是独立 GPIO。它与 LED 矩阵低边线 `P00/PA0` 复用，正常运行时由按键扫描程序在“输入上拉”和“LED 输出”之间切换。因此 U1 只有在关闭显示、停止矩阵扫描并把 `PA0` 固定为输入上拉后，才能把 S1 配置成 STOP 的下降沿唤醒源。

代码中的当前定义：

- U1 `WK_UP_GPIO_PORT = GPIOB`、`WK_UP_GPIO_PIN = GPIO_PIN_5`；
- U2 `PIN_WKUP_PORT = GPIOB`、`PIN_WKUP_PIN = LL_GPIO_PIN_7`；
- U2 `PIN_USBIN_PORT = GPIOA`、`PIN_USBIN_PIN = LL_GPIO_PIN_5`。

### 3.2 压力泵和主要外设

| 外设 | 引脚/资源 | 休眠处理 |
|---|---|---|
| 低压泵 | U2 `PA0/MOTO1/TIM1_CH1` | 停止 PWM，输出关闭电平 |
| 高压泵 | U2 `PA1/MOTO2/TIM1_CH2` | 停止 PWM，输出关闭电平 |
| IP2326 使能 | U2 `PA2/IP2326_EN` | 输出关闭电平 |
| 工作灯 | U2 `PB3` | 关闭 |
| 充电指示灯 | U2 `PC1` | 关闭或保持硬件要求的安全电平 |
| ADC | U2 `PA3/PA4/PB0/PB1` | 停止 ADC，关闭 ADC 时钟，GPIO 改为模拟输入 |
| WF183D 软件串口 | U2 `PB4/PB5`、`TIM14` | 停止软件串口和 TIM14 |
| U1/U2 控制串口 | U1 `PB4`、U2 `PA6/USART1` | 停止接收中断并关闭串口 |

## 4. 系统状态

系统使用以下逻辑状态：

| 状态 | 说明 |
|---|---|
| `RUNNING` | 正常显示、通信和外设任务运行 |
| `SLEEP_REQUESTING` | U1 已请求 U2 休眠，等待回复 |
| `SLEEP_PREPARED` | U2 已关闭不必要外设并允许休眠 |
| `STOP_SLEEP` | U1、U2 均已进入 STOP |
| `WAKE_RESTORING` | MCU 已被唤醒，正在恢复时钟和外设 |
| `DISPLAY_OFF` | MCU 已运行，但 U1 显示仍关闭，等待电源键操作 |
| `CHARGING_ACTIVE` | USB 已插入，U2 运行充电控制，系统保持唤醒 |

唤醒 MCU 和恢复正常显示是两个不同动作：

- 普通按键唤醒后，U1 可以先保持显示关闭；
- USB 唤醒后，U1 恢复显示并显示充电状态；
- 再次长按 S1，才退出 `DISPLAY_OFF` 并回到正常显示；
- 压力泵不会因为唤醒自动恢复运行。

## 5. 进入休眠条件

当前代码的休眠入口是 **U1 长按 S1**。长按事件只负责关闭显示并提交休眠请求，真正进入 STOP 仍要经过 U1/U2 握手和最后一次硬件状态检查。以下条件全部满足时，才允许进入 STOP：

- 没有正在处理的按键事件；
- U2 工作灯已关闭；
- USB 未插入且当前未充电；
- 压力泵已经停止；
- 压力传感器没有进行去皮、归零或校准；
- U1/U2 串口没有未完成事务；
- 没有正在进行唤醒或恢复流程。

当前版本没有启用“普通运行空闲 60 秒自动休眠”计时器；如果后续启用，必须复用同一个 `power_manager_request_sleep()` 入口，不能另写一套 STOP 流程。

用户主动长按 S1 时，U1 立即关闭显示并进入 `SLEEP_REQUESTING`。如果此时仍在充电、泵仍在运行或串口仍忙，系统只关闭显示，不进入 STOP；待条件满足后才会继续休眠。

S1 长按事件发生时，必须等待按键释放后才允许真正进入 STOP，避免用户仍按住按键时产生重复唤醒。

## 6. 进入休眠时序

### 6.1 U1 主控流程

```text
检测到 S1 长按或 60 秒空闲超时
        |
        v
关闭 U1 LED 输出
        |
        v
等待当前 UART 事务完成
        |
        v
发送 CMD_SLEEP 给 U2
        |
        +-- 返回 BUSY/ERROR --> 保持 U1 运行，稍后重试
        |
        +-- 返回 READY -------> 准备 U1 外设和唤醒引脚
                                      |
                                      v
                                  进入 STOP
```

### 6.2 U2 收到休眠命令后的流程

```text
收到 CMD_SLEEP
        |
        v
检查 USB、充电、泵和校准状态
        |
        +-- 不允许休眠 --> 返回 BUSY/ERROR
        |
        v
检查通过后回复 READY
        |
回复发送完成，U2 主循环提交休眠
        |
停止两路压力泵、ADC、TIM14、WF183D、USART1 和周期任务
        |
关闭 IP2326、工作灯和充电指示灯
        |
配置 PA5、PB7 为下拉唤醒输入
        |
再次检查唤醒电平和 EXTI 标志
        |
进入 STOP
```

U2 必须在回复帧发送完成后才执行 STOP，不能在 UART 发送过程中关闭串口时钟。

## 7. STOP 低功耗处理

建议使用 HAL 提供的 STOP/WFI 入口：

```c
HAL_SuspendTick();
HAL_PWR_EnterSTOPMode(PWR_LOWPOWERREGULATOR_ON,
                      PWR_STOPENTRY_WFI);
```

进入 STOP 前必须完成以下动作：

- 关闭不需要的外设输出；
- 停止外设运行和中断；
- 清除可能导致立即唤醒的中断挂起标志；
- 保留 GPIO、EXTI、PWR 和必要的系统配置时钟；
- 保留 U1 S1、U2 PA5、U1 PB5、U2 PB7 的唤醒配置；
- 关闭 SysTick 中断，避免每 1 ms 把 MCU 唤醒。

STOP 不是复位，也不会重新进入 `main()`。RAM 中的目标压力、模式、去皮值和显示缓存应保留。

当前应用没有初始化独立看门狗。如果后续启用看门狗，需要在 STOP 方案中增加看门狗的停止、低速运行或唤醒后重新配置策略。

## 8. 按键唤醒 U1，再唤醒 U2

### 8.1 时序

```text
休眠中，用户按下 U1 S1
        |
        v
U1 PA0 下降沿唤醒 U1
        |
        v
U1 恢复系统时钟和 GPIO
        |
        v
U1 将 PB5 临时改为推挽输出并拉高约 5 ms
        |
        v
U2 PB7 检测上升沿，唤醒 U2
        |
        v
U1 PB5 恢复为下拉输入
U2 PB7 恢复为下拉输入
```

### 8.2 按键唤醒后的业务规则

- 唤醒用的这次 S1 按键的按下、长按和释放事件全部屏蔽，只作为唤醒事件消费；
- 不允许因为唤醒动作直接启动或暂停压力泵；
- U1、U2 恢复外设后进入 `DISPLAY_OFF`；
- 用户释放 S1 后，再次长按 S1 才恢复显示；
- U1 恢复显示前，应确认 U2 已完成恢复或已响应状态查询。

## 9. USB 唤醒 U2，再唤醒 U1

### 9.1 时序

```text
休眠中，USB 插入
        |
        v
U2 PA5/USBIN 上升沿唤醒 U2
        |
        v
U2 恢复系统时钟和 GPIO
        |
        v
USB 按现有逻辑消抖确认
        |
        v
打开 IP2326，开始充电
        |
        v
U2 将 PB7 临时改为推挽输出并拉高约 5 ms
        |
        v
U1 PB5 检测上升沿，唤醒 U1
        |
        v
U2 PB7 恢复为下拉输入
U1 PB5 恢复为下拉输入
```

### 9.2 USB 唤醒后的业务规则

- USB 插入经过现有 20 ms 消抖确认后才算有效；
- U2 确认 USB 有效后打开 IP2326；
- U2 通过 `WK_UP` 唤醒 U1；
- U1 恢复显示并显示充电状态；
- 只要 USB 仍然插入，系统不允许再次进入 STOP；
- USB 唤醒不会启动压力泵。

## 10. 共享 WK_UP 线的安全规则

休眠时两边都必须是输入下拉，不能让 MCU 长时间驱动共享线。

唤醒脉冲使用以下规则：

1. 只有已经被本地事件唤醒的一方可以驱动共享线；
2. 驱动前先确认本地唤醒流程已经开始；
3. 只输出高电平，不主动输出低电平；
4. 高电平保持约 5 ms；
5. 脉冲结束后立即恢复输入下拉；
6. 另一方被唤醒后不能继续驱动该线；
7. 共享线默认必须回到低电平。

GPIO 模式切换时，应先设置输出数据寄存器为高，再切换到输出模式，避免切换瞬间产生低电平毛刺。

如果 U1 和 U2 同时被本地事件唤醒，两边可以同时产生高脉冲，但不能出现一边输出高、另一边输出低的情况；正常实现中两边都只允许输出高或输入下拉。

## 11. 唤醒恢复顺序

唤醒后不调用 `HAL_Init()`，也不重新执行完整上电流程。建议每颗 MCU 增加独立的恢复函数。

恢复顺序如下：

```text
清除唤醒中断标志
        |
        v
恢复 HSI、SYSCLK、AHB、APB 和 Flash 配置
        |
        v
恢复 SysTick，调用 HAL_ResumeTick()
        |
        v
恢复普通 GPIO 模式
        |
        v
恢复 UART 和 USART1 中断
        |
        v
恢复 U2 ADC
        |
        v
恢复 U2 TIM14 和 WF183D
        |
        v
恢复 U2 TIM1 PWM，但比较值保持 0
        |
        v
恢复 USB、充电和状态查询任务
        |
        v
回到主循环
```

### 11.1 U1 恢复内容

- 恢复系统时钟；
- 恢复 SysTick；
- 恢复 PB4 半双工 USART1；
- 恢复 LED 矩阵输出；
- 恢复按键扫描；
- 恢复 `PB5/WK_UP` 的输入下拉配置；
- 保留显示缓存，但根据业务状态决定是否点亮；
- 重新开始 U2 状态查询。

### 11.2 U2 恢复内容

- 恢复系统时钟；
- 恢复 SysTick；
- 恢复 PA6 USART1；
- 恢复 TIM14 和 WF183D 软件串口；
- 恢复 ADC；
- 恢复 TIM1 PWM，但 PA0/PA1 占空比初始化为 0；
- 恢复 PA5 USB 检测；
- 恢复 PB7/WK_UP 输入下拉；
- 恢复充电任务和状态查询任务；
- 不重新执行上电去皮，继续使用 RAM 中保存的去皮值。

## 12. 串口协议扩展

当前协议已经使用 `CMD=0x01~0x04`。新增：

```text
CMD=0x05：请求 U2 进入休眠
```

请求无载荷：

```text
AA 55 00 05 05
```

返回载荷长度为 1：

| 状态 | 含义 |
|---:|---|
| `0x00` | U2 已完成休眠准备，允许 U1 进入 STOP |
| `0x01` | 当前忙，暂时不能休眠 |
| `0x02` | 外设停止或休眠准备失败 |

返回帧格式为：

```text
AA 55 01 05 STATUS CHK
```

校验继续使用现有 `LEN + CMD + PAYLOAD` 的低 8 位累加和。

U1 等待现有协议使用的 100 ms 响应窗口。超时后：

- U1 不进入 STOP；
- 如果显示已经关闭，则保持显示关闭但继续运行；
- U1 可以通过 `WK_UP` 高脉冲唤醒可能已经进入 STOP 的 U2；
- 下一次安全时机重新发送休眠命令。

## 13. 软件模块拆分

建议新增以下模块，避免把低功耗代码散落到 `main.c` 和各个业务驱动中：

### U1

- `power_manager.c/.h`：休眠条件、U1 外设关闭、STOP 进入和恢复；
- `wake_manager.c/.h`：S1 唤醒、PB5 EXTI、WK_UP 高脉冲；
- `uart_command_driver.c/.h`：增加 `CMD_SLEEP` 请求和状态处理；
- `py32f002b_it.c`：增加按键和 WK_UP 的 EXTI ISR。

### U2

- `power_manager.c/.h`：休眠准备、外设关闭、STOP 进入和恢复；
- `wake_manager.c/.h`：PA5 USB 唤醒、PB7 EXTI、WK_UP 高脉冲；
- `uart_command_driver.c/.h`：处理 `CMD_SLEEP`；
- `uart_halfduplex_driver.c/.h`：增加休眠前停止接收和唤醒后重新初始化；
- `py32f002b_it.c`：增加 PA5/PB7 EXTI ISR。

中断函数只设置唤醒原因和标志，不在 ISR 中执行时钟、ADC、UART 或 GPIO 大量恢复操作。所有恢复动作放在主循环的 `power_manager_task()` 中完成。

## 14. 异常和安全处理

- U2 拒绝休眠时，U1 不进入 STOP；
- 休眠前必须把 PA0/PA1 PWM 比较值写成 0；
- 唤醒后 PA0/PA1 仍保持 0，直到收到新的启动泵命令；
- USB 插入检测必须经过消抖，不能由单个毛刺直接打开 IP2326；
- UART 发送未完成时不能关闭 USART1 时钟；
- STOP 前清除 EXTI 挂起标志，避免刚进入 STOP 就立即返回；
- 唤醒后重新配置 GPIO 前，先保证电机、IP2326 和 LED 为安全关闭状态；
- `WK_UP` 高脉冲结束后必须恢复输入下拉，防止线路长期保持高电平；
- 传感器去皮值和目标压力保存在 RAM 中，唤醒时不重复执行上电去皮；
- 如果任何恢复步骤失败，进入安全状态：压力泵关闭、IP2326 关闭、显示关闭，并保留调试错误状态。

## 15. 验证计划

### 15.1 进入休眠

- 空闲达到 60 秒后 U1/U2 都进入 STOP；
- S1 长按后显示关闭并快速进入 STOP；
- 压力泵运行时不能进入 STOP；
- USB 插入或正在充电时不能进入 STOP；
- UART 正在通信时不能中断事务进入 STOP。

### 15.2 按键唤醒

- 按下 S1 能唤醒 U1；
- U1 PB5 产生约 5 ms 高脉冲；
- U2 PB7 能检测上升沿并唤醒 U2；
- 唤醒后 PB5/PB7 都恢复为下拉输入；
- 唤醒不会启动压力泵；
- 释放 S1 后再次长按才能恢复显示。

### 15.3 USB 唤醒

- USB 插入能唤醒 U2；
- USB 消抖后 IP2326 正确打开；
- U2 PB7 能唤醒 U1；
- U1 能显示充电状态；
- USB 持续插入期间系统不会再次休眠；
- USB 插入毛刺不会触发反复唤醒和充电切换。

### 15.4 功耗和安全

- STOP 前确认 PA0/PA1、PA2、PB3、PC1 均为安全电平；
- ADC、TIM1、TIM14、USART1 和 SysTick 均已停止；
- 只有 GPIO、EXTI、PWR 和必要的唤醒时钟保持工作；
- 连续执行至少 100 次休眠/唤醒循环，不能出现串口失步、泵误启动或 WK_UP 长高；
- 测量正常运行、显示关闭但 MCU 运行、双 MCU STOP 三种状态的电流，确认功耗逐级下降。

## 16. 实现边界

本文是当前 U1/U2 工程的休眠与唤醒实现基线。真正改代码时，必须同时修改：

- U1/U2 的 `CMD_SLEEP` 协议定义；
- U1/U2 的 EXTI 和唤醒中断；
- U1/U2 的外设停止和恢复接口；
- U1/U2 的 STOP 入口和时钟恢复；
- U1 的长按显示状态机；
- U2 的 USB 唤醒和充电状态恢复；
- U1/U2 的协议和硬件联调文档。

实现完成的判断标准是：**休眠前所有非必要外设确实停止，唤醒后所有必要外设按顺序恢复，按键唤醒和 USB 唤醒能够分别完成双 MCU 联动，且压力泵不会误启动。**

## 17. 当前代码落点和验证结果

### 17.1 U1 代码

- `U1/Display/Src/power_manager.c`：S1/`PB5` EXTI、STOP 入口、显示/串口停启、唤醒恢复和 WK_UP 脉冲。
- `U1/Display/Src/display_control_driver.c`：S1 长按关闭显示并提交休眠请求。
- `U1/Display/Src/uart_command_driver.c`：增加 `CMD=0x05` 请求和返回校验。
- `U1/Display/Src/py32f002b_it.c`：增加 `EXTI0_1_IRQHandler` 和 `EXTI4_15_IRQHandler`。

### 17.2 U2 代码

- `U2/control/Src/power_manager.c`：USB/PB7 EXTI、STOP 入口、ADC/PWM/软件串口/控制串口停启、唤醒恢复和 WK_UP 脉冲。
- `U2/control/Src/uart_command_driver.c`：处理 `CMD=0x05`，先回复 ACK，再提交 STOP。
- `U2/control/Src/adc.c`、`moto.c`、`soft_uart.c`、`uart_halfduplex_driver.c`：增加 `DeInit/Suspend` 和对应恢复初始化，支持连续多次休眠唤醒。
- `U2/control/Src/py32f002b_it.c`：增加 `EXTI4_15_IRQHandler`。

### 17.3 已完成的离线验证

- U1/U2 修改后的应用层 C 文件已通过 ARM Cortex-M0+ 目标语法检查；新增低功耗模块和恢复路径无语法错误。当前 macOS 环境的 `arm-none-eabi-gcc` 缺少完整 `libc.a`，因此不能把本机离线检查表述为最终 ELF 链接通过。
- U1/U2 Keil `Project.uvprojx` XML 解析通过，新增 `power_manager.c/.h` 已进入 Application/User 文件组。
- 休眠请求帧、READY/BUSY 应答帧校验和，以及两条唤醒路径已完成主机模拟：

```text
休眠请求：AA 55 00 05 05
READY：  AA 55 01 05 00 06
BUSY：   AA 55 01 05 01 07
按键：   S1 -> U1 -> WK_UP -> U2
USB：    USB -> U2 -> WK_UP -> U1
```

### 17.4 必须上板确认的项目

离线编译和协议模拟不能替代硬件确认。烧录前后必须用示波器/逻辑分析仪和电流表确认：

1. U1 `PB5`、U2 `PB7` 休眠时确实为下拉输入，默认低电平；
2. S1 按下能产生 U1 `PA0` 下降沿，USB 插入能产生 U2 `PA5` 上升沿；
3. WK_UP 只有高脉冲，没有任何一方主动输出低电平；
4. STOP 电流明显低于显示关闭但 MCU 仍运行时的电流；
5. 连续至少 100 次“进入 STOP -> 按键唤醒”和“进入 STOP -> USB 唤醒”循环，泵、IP2326、串口和显示均无误动作。
