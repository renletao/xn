# U1/U2 电池电量、充电状态及显示适配方案

## 1. 目的和范围

本文档定义当前 U1 Display + U2 Control 工程适配新的电池电量、充电状态和显示规则时的实现方案。

本方案只定义行为、模块职责、数据格式、时序和验收条件，不包含具体代码实现。适配完成后，电量不再表示真实的 0~100% 容量，而表示基于电池端 ADC 电压分档得到的四级状态。

## 2. 总体架构

```text
U2
  ADC 原始采样
    -> 2 点平均
    -> 64 点平均 TEMPADC
    -> 按工作状态选择 ADC 阈值
    -> BatLevel 单向状态机

  USB_IN 充电输入
    -> 现有 USB 消抖 + 充电电流判断
    -> ChargeState / f_charging

  BatLevel + ChargeState
    -> 低电保护
    -> 充电时禁止电机
    -> CMD=0x02 上报 U1

U1
  接收 CMD=0x02
    -> 记录 BatLevel 和 ChargeState
    -> 沿用当前显示总开关决定显示内容
    -> 将 BatLevel 转换为电池图标格数
    -> 处理充电闪烁和当前显示关闭逻辑
```

原则如下：

1. U2 是电量和充电状态的唯一业务来源。
2. U1 不直接读取电池 ADC，也不自行推断电量百分比。
3. U2 本地执行低电停机和 USB 电机保护，不能依赖 U1 的串口周期查询来保证安全。
4. U1 的显示模式不改变 U2 的电池判断结果，只改变显示方式。

## 3. 电量等级定义

### 3.1 `BatLevel` 含义

`BatLevel` 使用目标工程定义，数值越小表示电量越高：

| `BatLevel` | 业务含义 | 电池图标格数 |
|---:|---|---:|
| 0 | 满电档 | 3 格 |
| 1 | 较高电量档 | 2 格 |
| 2 | 较低电量档 | 1 格 |
| 3 | 空电/低电档 | 0 格，仅外框 |

当前 U1 图标驱动内部使用“0~3 格”的正向含义，因此显示层必须使用：

```text
display_level = 3 - BatLevel
```

不得把 `BatLevel` 直接当作图标格数传给 U1。

### 3.2 ADC 阈值表

阈值是 12 位 ADC 原始码，不是电压值，也不是百分比。

| 工作状态 | 第 1 阈值 | 第 2 阈值 | 第 3 阈值 |
|---|---:|---:|---:|
| 静置 `NorAD` | 1650 | 1544 | 1393 |
| 电机运行 `WorkAD` | 1393 | 1262 | 1220 |
| 充电 `ChargeAD` | 1725 | 1619 | 1469 |

判定顺序：

| 条件 | 新采样对应的等级 |
|---|---:|
| `TEMPADC >= AD[0]` | 0 |
| `AD[1] <= TEMPADC < AD[0]` | 1 |
| `AD[2] <= TEMPADC < AD[1]` | 2 |
| `TEMPADC < AD[2]` | 3 |

电机运行时使用较低的 `WorkAD` 阈值，以抵消电机大电流造成的电池端电压下降。判断工作状态时，应使用 U2 已确认的电机运行状态，而不是 U1 的本地缓存。

### 3.3 ADC 采样和平均

目标采样链路为：

1. ADC 选择电池分压输入，对应硬件定义的 `AI0-AGND`。
2. ADC 原始采样频率设置为 100 Hz，即每 10 ms 读取一次 12 位原始值。
3. 连续两个原始值求平均，得到一个有效 ADC 平均值，周期约 20 ms。
4. 累计 64 个有效 ADC 平均值后再次求平均，得到 `TEMPADC`，周期约 1.28 s。
5. 只有完成一次 64 点平均，才提交一次新的电量判断结果。

因此一次 `TEMPADC` 实际使用 128 个 ADC 原始转换值。电池等级不要根据单次 ADC 中断结果直接改变。

主循环可以高频调用 ADC 任务，但任务内部必须使用 10 ms 的节拍门控，不能把主循环执行次数直接当作采样次数。ADC 读取可以使用硬件定时器或现有 ADC 调度机制完成。

系统启动阶段先连续完成 128 个原始 ADC 采样，得到第一组 `TEMPADC` 并初始化 `BatLevel`，之后才进入 `while` 主循环。这样进入正常业务循环时已经有有效的初始电量等级。

如果电机在一个 64 点窗口中启动或停止，建议丢弃当前未完成窗口，从新的工作状态重新累计，避免把静置电压和带载电压混在同一个 `TEMPADC` 中。

## 4. `BatLevel` 单向变化状态机

### 4.1 正常更新规则

每完成一次有效 `TEMPADC` 后，根据当前状态得到 `adc_level`，再按下列规则更新 `BatLevel`：

| 当前状态 | 允许的变化 |
|---|---|
| USB 已插入且检测到充电电流 `CHARGING` | 只允许向满电方向变化：`3 -> 2 -> 1 -> 0` |
| USB 未插入 `IDLE` | 只允许向低电方向变化：`0 -> 1 -> 2 -> 3` |
| USB 已插入但未检测到充电电流 | 保持当前等级，不根据本次采样改变方向 |

建议每次有效平均最多变化一级，而不是一次跨越多个等级。这样可以避免一次异常采样直接从满电跳到空电，也使显示变化可控。

示例：

- 当前为 `BatLevel=2`，非充电时 ADC 只短暂达到 1 档，仍保持 2；
- 当前为 `BatLevel=2`，充电时 ADC 达到 1 档，允许变为 1；
- 当前为 `BatLevel=2`，充电时 ADC 仍为 3 档，不允许变差为 3。

### 4.2 上电初始化

上电后第一次完成 64 点平均时，应直接用当前 ADC 结果初始化 `BatLevel`，不执行单向限制。否则如果默认等级为 3，设备实际满电时会被“非充电只允许变差”规则永久卡在低电档。

初始化完成后，才启用单向变化规则。

### 4.3 充电状态切换时的处理

- 从未充电切换为正在充电：保留当前等级，后续只允许向 0 变化。
- 从充电状态切换为未充电：从下一次有效 `TEMPADC` 起允许向低电方向变化。

## 5. 充电状态机

### 5.1 USB 是主判据

充电状态同时使用 U2 已消抖的 `USB_IN` 和电流检测结果：USB 是必要条件，电流方向用于确认是否真的处于充电状态。

| USB_IN | 充电电流 | 确认状态 | 标志和动作 |
|---:|---|---|---|
| 0 | 任意 | `IDLE` | 清除 `f_charging` |
| 1 | 检测到充电电流 | `CHARGING` | 设置 `f_charging=1` |
| 1 | 未检测到充电电流 | `IDLE` | 清除 `f_charging`，但继续保持 USB 电机保护 |

当前电流方向约定沿用已有 ADC 逻辑：电流为负表示电流流入电池，视为正在充电；电流为零或正表示未检测到充电电流。USB 拔出后立即结束充电状态。

已确认状态使用以下枚举：

```text
IDLE     USB 未插入
CHARGING USB 已插入且检测到充电电流
```

`f_charging` 由这个确认状态派生。

## 6. 电机控制和低电保护

### 6.1 充电禁止启动

当 USB 已确认插入时，U2 拒绝新的电机启动请求，并返回明确的“USB/充电中禁止启动”结果。只要 USB 仍然插入，就保持电机禁止，避免外部电源和电机同时工作。

禁止条件应使用 U2 本地已经消抖的 USB 状态和电机状态，不依赖 U1 的串口缓存，也不依赖 U1 是否处于显示关闭状态。

如果电机已经运行后才检测到 USB 插入，应立即停止电机，并清除运行标志。USB 拔出后，只有在低电保护未锁定的情况下才允许重新启动。

### 6.2 低电保护

触发条件：

1. USB 未插入；
2. 已完成一次 64 点平均；
3. 当前 `BatLevel >= 3`；
4. 本次 `TEMPADC` 仍然落在低电档。

触发动作：

1. 立即停止所有电机输出；
2. 清除电机运行状态；
3. 进入 `LO` 低电保护状态；
4. 向 U1 上报低电等级和当前充电状态；
5. 当前版本不增加额外 3 秒确认延时。

低电保护必须由 U2 本地触发，不能等待 U1 每秒一次的状态查询。

`LO` 的解除规则：

1. USB 插入后立即解除 `LO` 锁定，但 USB 插入期间仍禁止电机；
2. USB 拔出后，只有 `BatLevel<=2` 时才允许重新启动电机；
3. 如果 USB 拔出时仍为 `BatLevel=3`，继续保持低电保护。

### 6.3 充电最长时间

最长充电计数使用独立于 ADC 平均的 1.024 秒计时节拍：

```text
21600 次 × 1.024 秒 = 22118.4 秒 ≈ 6.14 小时
```

超时后不直接把电量设为满电，而是每次按一个等级逐步向满电方向调整：

```text
3 -> 2 -> 1 -> 0
```

充电超时计数必须在确认进入 `CHARGING` 时开始，在 USB 拔出、充电电流消失或系统复位时清零。

ADC 平均周期约为 1.28 秒，仍不能把它直接当作 1.024 秒充电计数节拍；最长充电计数继续使用独立的 1.024 秒节拍。

## 7. U1/U2 协议方案

### 7.1 采用协议版本 2 的 `CMD=0x02`

建议保留当前 `CMD=0x02` 的 7 字节返回长度，避免改变帧长度和压力/泵字段位置，但修改字段语义，并要求 U1、U2 同时升级。

| 字段 | 偏移 | 长度 | 新含义 |
|---|---:|---:|---|
| `CHARGE_STATE` | 0 | 1 | `0=未检测到充电`，`1=USB已插入且检测到充电电流` |
| `BAT_LEVEL` | 1 | 1 | `0~3`，使用目标 `BatLevel` 含义 |
| `PRESSURE` | 2 | 4 | Pa，小端序，保持现有定义 |
| `PUMP` | 6 | 1 | `0=停止`，`1=运行`，保持现有定义 |

USB 拔出优先级最高，直接发送状态 0。

U1 收到数据后：

1. 验证 `CHARGE_STATE` 是否为 0 或 1；
2. 验证 `BAT_LEVEL` 是否为 0~3；
3. 将 `BAT_LEVEL` 转换为 `display_level=3-BAT_LEVEL`；
4. 根据充电状态和当前已有的显示总开关决定静态显示或动画显示；
5. 无效帧、超时或通信失败时保留最近一次有效显示。

协议字段名必须同步修改为 `BAT_LEVEL`，不能继续叫 `BATTERY_PERCENT`，避免调试和维护时误认为是百分比。

### 7.2 协议兼容策略

由于原协议的电池字段是 0~100%，而新协议是 0~3 的反向等级，不能让旧版 U1 和新版 U2 混用：

- 将协议说明明确标记为版本 2；
- U1、U2 作为一个整体同时升级；
- 启动阶段不根据电池字段范围自动猜测协议版本；

## 8. U1 显示适配（保持当前显示模式）

本次适配不新增 `DisMode=0/3/4` 状态机，也不改变当前按键、显示总开关和电源管理的组织方式。继续使用现有的：

- `led_is_enabled()` / `led_set_enabled()` 作为显示总开关；
- `display_control_scan()` 作为显示业务汇总入口；
- `battery_icon_driver` 只负责电池图标行；
- `power_manager` 负责当前的显示关闭、唤醒和硬件 STOP。

适配只增加两项显示输入：

1. 从协议接收 `BatLevel` 和 `ChargeState`；
2. 将 `BatLevel` 转换为当前图标驱动需要的正向格数。

现有普通显示行为保持不变：

1. 显示总开关打开且未充电：静态显示当前电量。
2. 显示总开关打开且 `CHARGING`：播放充电闪烁。
3. 显示总开关关闭：矩阵全部关闭，但保留电量和充电状态；重新打开时立即按当前状态刷新。
4. 低电 `BatLevel=3` 时，普通显示只显示电池外框；不新增独立的低电显示模式。

不修改当前压力、模式、单位和数字显示的显示模式逻辑。电池行的更新继续通过单行缓存接口完成，不能清除或覆盖其他显示行。

### 8.1 充电闪烁显示

按照目标 `BatLevel` 显示以下两个图标状态：

| `BatLevel` | 闪烁的两个显示状态 |
|---:|---|
| 0 | 三格 / 两格 |
| 1 | 三格 / 两格 |
| 2 | 两格 / 一格 |
| 3 | 一格 / 空外框 |

将 25 个电量任务周期定义为一次状态保持时间。按约 20 ms 计算约为 500 ms，完整亮灭循环约为 1 秒。

### 8.3 低电显示

非充电且 `BatLevel=3` 时，按当前显示开关规则显示电池外框：显示打开时显示外框，显示关闭时全部关闭。不增加额外的低电闪烁模式，避免改变现有显示模式。

进入 `CHARGING` 后，由充电动画接管电池图标；USB 拔出后，如果当前仍为低电档，则回到静态空电外框显示。

## 9. 电源管理约束

保留当前电源管理行为，不为电池适配新增“休眠充电显示”模式：

1. 当前显示关闭仍由 `led_set_enabled(0)` 执行，矩阵输出全部关闭。
2. 当前硬件 STOP 仍由 `power_manager` 在满足现有条件后执行。
3. USB 已插入时不进入真正的硬件 STOP，U2 保持充电状态检测和电机保护运行。
4. 不修改当前压力/数字显示在显示关闭或硬件 STOP 时的处理方式。

## 10. 本版实施边界

本版方案只纳入已经确认的行为：

1. 电池按 ADC 原始码分档，不计算真实百分比。
2. 充电检测同时使用 USB 插入状态和充电电流，USB 是必要条件。
3. USB 插入期间禁止启动电机，运行中检测到 USB 也要停机。
4. 非充电低电时由 U2 本地执行电机保护。
5. U1 沿用当前显示总开关、矩阵扫描和电源管理，不新增显示模式状态机。

本版不扩展当前工程尚未定义的其他充电状态、显示模式或低电恢复状态；这些内容等确认后再单独加入，不作为本版实现前提。

## 11. 实施顺序

建议按以下顺序实施，每一步完成后再进入下一步：

1. 按现有硬件配置确认 ADC、USB_IN、分压和阈值的对应关系。
2. 在 U2 建立原始 ADC 采样、2 点平均和 64 点平均链路。
3. 加入 `NorAD`、`WorkAD`、`ChargeAD` 三套阈值和 `BatLevel` 单向状态机。
4. 以 USB_IN 和充电电流建立 `IDLE/CHARGING` 状态。
5. 在 U2 加入 USB 期间电机保护、低电停机、LO 状态和最长充电计数。
6. 将 `CMD=0x02` 升级为协议版本 2，同时修改 U1/U2 的校验和语义。
7. 在 U1 现有显示控制流程中加入 `BatLevel` 反向映射。
8. 替换现有充电动画为目标的两状态闪烁动画。
9. 最后验证现有显示总开关、唤醒和硬件 STOP 不被电量适配破坏。

## 12. 验收测试矩阵

### 12.1 电量分档

分别在三套阈值表下输入略高于、等于、略低于三个阈值，确认档位边界为：

```text
>= 第 1 阈值 -> BatLevel 0
>= 第 2 阈值 -> BatLevel 1
>= 第 3 阈值 -> BatLevel 2
<  第 3 阈值 -> BatLevel 3
```

### 12.2 单向变化

1. 非充电时从 0 档逐步降低输入，确认只能 `0→1→2→3`。
2. 充电时从 3 档逐步升高输入，确认只能 `3→2→1→0`。
3. 在阈值附近施加抖动，确认不会来回跳档。
4. 上电第一次有效平均确认可以直接建立正确初始等级。

### 12.3 充电状态

1. USB 未插入时进入未充电。
2. USB 插入但未检测到充电电流时保持未充电，同时启用 USB 电机保护。
3. USB 插入且检测到充电电流后进入充电状态。
4. USB 拔出或充电电流消失时清除充电标志。

### 12.4 显示

1. `BatLevel=0/1/2/3` 分别显示 3/2/1/0 格。
2. 现有显示总开关打开且未充电时静态显示，不闪烁。
3. 正在充电时显示目标两状态闪烁。
4. `BatLevel=3` 且显示打开时只显示电池外框。
5. 显示总开关关闭时矩阵全部关闭，重新打开后恢复当前电量状态。
6. 硬件 STOP 和唤醒流程保持现有行为。

### 12.5 保护

1. USB 插入期间启动电机被拒绝。
2. 电机运行中检测到 USB 插入时立即停机。
3. 非充电低电确认后立即停机并进入 LO。
4. 低电保护状态下启动电机被拒绝。
5. 充电超时按等级逐步向满电方向变化。
6. 串口超时或错误不会让 U1 把电量清零。

## 13. 结论

本方案将电量判断、以 USB 为主的充电状态和低电保护集中在 U2，将 `BatLevel` 的图标映射和动画接入 U1 现有显示流程，并通过协议传递明确的 `BatLevel` 与 `ChargeState`。电机保护由 U2 本地保证，显示模式继续沿用当前工程的显示总开关、矩阵扫描和电源管理，不新增独立的 `DisMode` 状态机。

本版实现以现有硬件配置和本文已定义的 USB 主判据为边界，后续新增状态或显示模式不在本版范围内。
